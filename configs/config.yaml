# Local: rps-maestro/configs/config.yaml
server:
  port: 8000

database:
  host: "postgres"
  port: 5432
  user: "user"
  password: "password"
  dbname: "maestro_db"

rabbitmq:
  host: "rabbitmq"
  port: 5672
  user: "guest"
  password: "guest"

worker:
  # Deixe vazio para desabilitar autenticação em desenvolvimento.
  # Em produção, defina via variável de ambiente MAESTRO_WORKER_API_KEY.
  apikey: ""